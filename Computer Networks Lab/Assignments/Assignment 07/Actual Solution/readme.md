#### Solution without using connect() and https://www.geeksforgeeks.org/hamming-code-in-computer-network/

#### Check the udp ports involved while running the programs
`netstat -a -p -n --numeric-ports -u`
